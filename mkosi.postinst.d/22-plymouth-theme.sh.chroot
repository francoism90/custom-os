#!/bin/sh -eux

# Set default Plymouth theme for a polished boot experience
KEEP_THEME="breeze-bgrt"

if command -v plymouth-set-default-theme >/dev/null 2>&1; then
    plymouth-set-default-theme "$KEEP_THEME" || true
fi

exit 0
